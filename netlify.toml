[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["src/brand.config.js"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
